#include <stdio.h>
#include <conio.h>
int main()
{
int n,reverse=0,remanider;
printf("Enter an integer:");
scanf ("%d",&n);
while (n!=0)
{
remanider=n%=10;
reverse = reverse*10+remanider;
n /=10 ;
}
printf("Reversed number=%d",reverse);
return 0 ;
}

